import { Paper } from "@mui/material";

const PageSearch = () => {

    return (
        <Paper elevation={2}
               sx={{
                   p: 2,
                   display: 'flex',
                   flexDirection: 'column',
                   height: 'auto',
               }}
        >
            RICERCA<br/>
            Work in progress...
        </Paper>
    );
};

export default PageSearch;